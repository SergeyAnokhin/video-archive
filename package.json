{
  "name": "video-archive",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first Windows development workspace for the Video Archive frontend and backend.",
  "scripts": {
    "dev": "concurrently -k -n frontend,backend -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

