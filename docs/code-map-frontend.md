# Code Map — Frontend

Part of the [code map](code-map.md). **Update this file whenever frontend files are added, moved, or removed.** Keep entries short — what lives there and when to go there; rationale and history live in git.

## `frontend/` — Vite + React + TypeScript

| Path | Role |
| --- | --- |
| [`frontend/package.json`](../frontend/package.json) | Scripts (`dev`, `build`, `lint`, `preview`, `test` — vitest) and deps (React, i18next, react-i18next, `lucide-react`). |
| [`frontend/vite.config.ts`](../frontend/vite.config.ts) | Dev server on `5173` (`PORT` override), host `0.0.0.0`; proxies `/api` to the backend on `127.0.0.1:8000` (`BACKEND_PORT` override). |
| [`frontend/index.html`](../frontend/index.html) | HTML shell; `data-theme` on the root selects the active theme variable set. |
| [`frontend/src/main.tsx`](../frontend/src/main.tsx) | Entry point; wires global styles and i18n before rendering `App`. |
| [`frontend/src/App.tsx`](../frontend/src/App.tsx) | Root component: `InterfaceSettingsProvider` (outermost) → `PreviewVisibilityProvider` → `AppLayout`. |
| [`frontend/src/styles/theme.css`](../frontend/src/styles/theme.css) | CSS variable sets for all eight theme presets, including `--color-accent-2` and per-preset glow/pulse rules gated behind `prefers-reduced-motion`. |
| [`frontend/src/styles/global.css`](../frontend/src/styles/global.css) | Reset, base typography/touch-target rules, and theme-following scrollbar styling. |
| [`frontend/src/components/TopBar.tsx`](../frontend/src/components/TopBar.tsx) (+ `.css`) | Top bar: menu toggle, title, `BackendHealthWidget`, centered `LibrarySearchBox`, then icon buttons (activity, Jobs, preview-visibility eye, theme cycle, Settings). `activeSearch` state lives in `AppLayout.tsx`; when space tightens the health widget hides first (640px), the title later (480px). |
| [`frontend/src/components/BackendHealthWidget.tsx`](../frontend/src/components/BackendHealthWidget.tsx) (+ `.css`) | Groups `BackendStatusDot` + `BackendResourceMeter`, owning the single `useBackendHealth`/`useBackendStats` polling pair; loads the user-configured timeouts via `useBackendHealthSettings.ts`. |
| [`frontend/src/components/BackendStatusDot.tsx`](../frontend/src/components/BackendStatusDot.tsx) (+ `.css`) | Status dot next to the title: green (healthy) / amber (`'slow'`) / red (offline); no visible label — state is in the `title` tooltip. |
| [`frontend/src/components/BackendResourceMeter.tsx`](../frontend/src/components/BackendResourceMeter.tsx) (+ `.css`) | CPU/memory/network(SMB) gauge fed by `GET /api/system/stats`; click cycles four display modes persisted in `localStorage['backendResourceMeter.mode']`; exact values always in `title` tooltips. |
| [`frontend/src/components/LibrarySearchBox.tsx`](../frontend/src/components/LibrarySearchBox.tsx) (+ `.css`) | Scoped search box: parses `tag:`/`file:`/`path:` prefixes via `utils/searchQuery.ts`; capped suggestion groups (tags client-side from `GET /api/tags/used`, files/folders via debounced server queries); submits an `ActiveSearch` upward or navigates directly on a folder pick. |
| [`frontend/src/components/SearchResults.tsx`](../frontend/src/components/SearchResults.tsx) (uses `LibraryView.css`) | Search-results view: three capped groups (unscoped) or one flat infinite-scroll grid (scoped, 60/page). Shares `sortFiles()` with `LibraryView.tsx` and reports on-screen order up via `onFilesChanged` for prev/next navigation. |
| [`frontend/src/components/SettingsModal.tsx`](../frontend/src/components/SettingsModal.tsx) (+ `.css`) | Tabbed settings modal driven by a fixed `TABS` array (icon sidebar on desktop, icon strip / full-screen sheet below 640px). Shared utilities: `.settings-modal__option(--icon)` and `.settings-modal__row`. |
| [`frontend/src/components/SourceSection.tsx`](../frontend/src/components/SourceSection.tsx) | Source connection form: `local`/`smb` toggle, test-connection, connect/replace with confirmation, SMB Reconnect, and a post-connect restore offer when `detected_backups` is non-empty. Editing the active SMB source's connection fields while `root_path` stays the same (`isConnectionOnlyEdit`) skips the replace confirmation and saves as an in-place reconnect (mirrors the backend's own `PUT /source` reconnect check). Passively fetches `GET /source/status` (SMB only) to show live connectivity + direct-access-active/fallback as a hint under the current-source line. |
| [`frontend/src/components/SavedSourcesSection.tsx`](../frontend/src/components/SavedSourcesSection.tsx) | Saved-sources list (`GET /api/sources`): per row — active badge, preview-cache size/count, switch (`POST /activate`), clear-preview-cache, forget (disabled for the active source). |
| [`frontend/src/components/PlaybackSettingsSection.tsx`](../frontend/src/components/PlaybackSettingsSection.tsx) | Single `stream`/`direct_link` mode selector (`GET/PUT /api/playback-settings`). |
| [`frontend/src/components/PerformanceSettingsSection.tsx`](../frontend/src/components/PerformanceSettingsSection.tsx) | Single `parallel_workers` input (1-16, `GET/PUT /api/performance-settings`). |
| [`frontend/src/components/NetworkAccessSection.tsx`](../frontend/src/components/NetworkAccessSection.tsx) | Settings → Network: lists `http://<lan-ip>:<port>` per detected address (`GET /api/app/network-info`) with copy buttons, plus LAN/hotspot/firewall instructions. |
| [`frontend/src/components/BackendHealthSettingsSection.tsx`](../frontend/src/components/BackendHealthSettingsSection.tsx) | Settings → Network (below `NetworkAccessSection`): slow/offline timeout inputs, `GET/PUT /api/backend-health-settings`. Always PUTs both fields together — the backend's per-field defaults would silently reset an omitted one. |
| [`frontend/src/components/ConversionProfilesSection.tsx`](../frontend/src/components/ConversionProfilesSection.tsx) | Conversion profile CRUD via `useConversionProfiles()`; includes the internal `ConversionProfileForm` and `MinSizeReductionSetting` (global `GET/PUT /api/conversion-settings`, not per-profile). |
| [`frontend/src/components/PreviewSettingsSection.tsx`](../frontend/src/components/PreviewSettingsSection.tsx) (+ `.css`) | Preview settings, two sub-tabs: **Collage** (aspect ratio, construction-set grid editor — tile geometry from `POST /api/preview-layouts/preview`, backend canonical — preset gallery, save/load slots) and **Animated preview** (frame count, GIF width/colors, `frame`/`clip` source mode, segment duration, transition). Persisted via `/api/preview-settings`. |
| [`frontend/src/components/LayoutCanvas.tsx`](../frontend/src/components/LayoutCanvas.tsx) | Black-canvas grid renderer for the layout editor (styles in `PreviewSettingsSection.css`); doubles as the interactive editor surface and `mini` gallery thumbnails. |
| [`frontend/src/components/TaggingSettingsSection.tsx`](../frontend/src/components/TaggingSettingsSection.tsx) | AI vocabulary editor (shared `TagChipEditor`) plus frame-count, top-tag count, collage toggle, shared `image_resolution`, and request-timeout fields (`/api/tagging-settings`); "preview what the model sees" via `POST /api/tagging-settings/preview`. Renders `UserDefinedTagsSection` below; covered by `TaggingSettingsSection.test.tsx`. |
| [`frontend/src/components/TagChipEditor.tsx`](../frontend/src/components/TagChipEditor.tsx) | Reusable tag-chip CRUD list built on `TagBadge`: toggle/rename/recolor/delete/copy-all/add. Shared by `TaggingSettingsSection` (AI pool) and `UserDefinedTagsSection` (user pool); the caller passes `createDefaults` so "add" lands in the right pool. |
| [`frontend/src/components/TagBadge.tsx`](../frontend/src/components/TagBadge.tsx) (+ `.css`) | The single shared tag-rendering component: colored background (`tag.color` or `hashTagColor()`) with contrast-aware text (`utils/tagColor.ts`); optional swatch, remove `×`, score label, confirmed pulse. Used by every tag-rendering surface. |
| [`frontend/src/components/UserDefinedTagsSection.tsx`](../frontend/src/components/UserDefinedTagsSection.tsx) | Settings section for the user-defined tag pool: `GET /api/tags?category=user` + `TagChipEditor` with `createDefaults={{is_ai_vocabulary:false, is_user_defined:true}}`. |
| [`frontend/src/components/ProviderSettingsSection.tsx`](../frontend/src/components/ProviderSettingsSection.tsx) | Priority-ordered provider entry list: reorder (`POST /reorder`), enable toggle, inline `ProviderEntryForm` edit, per-row "Advanced settings" (`text_model`, `batch_enabled`, `PriceOverrideFields`), delete, export/import (export includes plaintext keys behind a confirm). Renders `ModelPricingSection` and `ProviderUsageSection` below. |
| [`frontend/src/components/ProviderEntryForm.tsx`](../frontend/src/components/ProviderEntryForm.tsx) | Create/edit form for one provider entry; "Load models" feeds a `<datalist>` (`POST .../models`; hardcoded `FAL_VISION_MODELS` for `fal`). |
| [`frontend/src/components/ModelPricingSection.tsx`](../frontend/src/components/ModelPricingSection.tsx) | Editable `$/1M` price table (`PUT /api/settings/model-pricing`) + "Refresh from OpenRouter"; `prices` state is lifted into `ProviderSettingsSection` (shared with `PriceOverrideFields`). |
| [`frontend/src/components/ProviderUsageSection.tsx`](../frontend/src/components/ProviderUsageSection.tsx) | Read-only usage-statistics table (`GET /api/settings/provider-usage`): calls, tokens, estimated cost, last used — per provider/model. |
| [`frontend/src/components/PriceOverrideFields.tsx`](../frontend/src/components/PriceOverrideFields.tsx) | Per-entry `$/1M` price override in the "Advanced settings" panel; writes the same `model_pricing` row as `ModelPricingSection`. |
| [`frontend/src/components/providerCatalog.ts`](../frontend/src/components/providerCatalog.ts) | Shared provider constants: `PROVIDER_TYPES`, `MODEL_LISTING_SUPPORTED`, `BATCH_SUPPORTED` (mirrors the backend's `registry.entry_supports_batch`), `FAL_VISION_MODELS`. |
| [`frontend/src/components/BackupMaintenanceSection.tsx`](../frontend/src/components/BackupMaintenanceSection.tsx) | Backup list with Restore/Delete, create-backup (`include_secrets`), retention count, and the three maintenance triggers; watches `useJobs()` to refresh when its job finishes. |
| [`frontend/src/components/AppSettingsExportSection.tsx`](../frontend/src/components/AppSettingsExportSection.tsx) | Settings → Backup, below `BackupMaintenanceSection`: download/upload of the `GET`/`POST /api/settings/app-settings/export`\|`import` bundle (no secrets, so no export confirm); import is behind a `window.confirm` and reports applied counts. |
| [`frontend/src/components/InterfaceSection.tsx`](../frontend/src/components/InterfaceSection.tsx) | Language picker, 8-preset theme picker, search-limit inputs, and two `PreviewProfileEditor` blocks (live sample thumbnails via `previewFilterCss()`). All via `useInterfaceSettings()`. |
| [`frontend/src/components/JobsModal.tsx`](../frontend/src/components/JobsModal.tsx) (+ `.css`) | Jobs modal: status-grouped cards with progress bar, current item, elapsed, ETA (`useEtaSeconds`), `failed_item_count`; per-card cancel/pause/resume/restart/remove, view-log (filtered `LogViewerModal`), and a `BatchSubmissionsModal` header button. Cards never show raw ids — scope/current-item lines show basenames (`scope_label`/`item_key`, full path in tooltip) and click-navigate via `onNavigate()`; a running item's `progress_pct` renders as a second finer bar. |
| [`frontend/src/components/BatchSubmissionsModal.tsx`](../frontend/src/components/BatchSubmissionsModal.tsx) (+ `.css`) | Read-only list of still-polling batch submissions (`GET /api/jobs/batch-submissions`, 5s refresh) with per-row "forget locally" (no provider-side cancel). |
| [`frontend/src/components/LogViewerModal.tsx`](../frontend/src/components/LogViewerModal.tsx) (+ `.css`) | Log viewer: job/file/level filters, backfill from `GET /api/logs` + live `EventSource` on `/api/logs/stream`, capped at 500 events, copy-all. Rows show elapsed `MM:SS` timestamps and a clickable `#N` file badge that toggles the file filter. |
| [`frontend/src/components/AppLayout.tsx`](../frontend/src/components/AppLayout.tsx) | Shell: top bar + collapsible directory-tree pane (closed by default; drawer overlay below 640px) + main content + modals. Owns the selected-folder path and the `activeSearch` state. |
| [`frontend/src/components/DirectoryTree.tsx`](../frontend/src/components/DirectoryTree.tsx) (+ `.css`) | Recursive expandable folder tree (`GET /api/tree?include_status=true&include_top_tags=true`) with status and `top_tags` dots; the fetched root node itself is never rendered. Toolbar: name filter (prunes to matching branches) and expand-all/collapse-all. |
| [`frontend/src/components/LibraryView.tsx`](../frontend/src/components/LibraryView.tsx) (+ `.css`) | The library screen: fetches `GET /api/directories/children` for the selected path, or delegates to `SearchResults.tsx` during a search. Toolbar (breadcrumb, "Recent folders" quick-jump, create-folder, sort-cycle, rescan, folder-scope convert/preview/tag); opens `PlaybackModal`/`ImageViewerModal` with prev/next from the current sorted order; reloads in place on relevant job/file changes; records recently-viewed history. |
| [`frontend/src/components/LibraryCards.tsx`](../frontend/src/components/LibraryCards.tsx) | `FolderCard`/`FileCard` (styles in `LibraryView.css`): GIF thumbs (collage/image fallback), status dots, size/tags rows, `top_tags` badges, favorite star, duration badge, variant/tuning markers, up to 4 AI-tag `TagBadge` chips, per-card delete with confirm. |
| [`frontend/src/components/FileInfoPanel.tsx`](../frontend/src/components/FileInfoPanel.tsx) (+ `.css`, shares `ConvertDialog.css`) | Near-fullscreen per-file panel: still collage/image (click opens an in-component lightbox — the picture, not playback), editable tags with colored suggestions, `UserDefinedTagButton`, ffprobe media-info grid (`GET /api/files/{id}/media-info`, warning banner on `probe_failed`), and one button per action (play/preview/convert/tune video-only; tag/similar/move/delete always). Draggable collage/details divider persisted to `localStorage`; optional prev/next + `FolderQuickActions`. |
| [`frontend/src/components/SimilarFilesModal.tsx`](../frontend/src/components/SimilarFilesModal.tsx) (+ `.css`, shares `ConvertDialog.css`) | Lists `GET /api/files/{id}/similar` matches (path + bit difference); empty list is a normal outcome. Standalone images work too (same-kind-only, backend-enforced). |
| [`frontend/src/components/PlaybackModal.tsx`](../frontend/src/components/PlaybackModal.tsx) (+ `.css`) | Full-viewport playback overlay: HTML5 `<video key={file.id}>` against the stream URL (the `key` forces a fresh mount — a `<source>` src change alone doesn't reload), or copyable direct path + raw-stream link in `direct_link` mode. Prev/next chevrons + capture-phase arrow keys, `FolderQuickActions`, tag-add cluster (`QuickTagAdd`/`UserDefinedTagButton`, one shared open state), info and delete buttons. Video only — `ImageViewerModal` is the image counterpart. |
| [`frontend/src/components/ImageViewerModal.tsx`](../frontend/src/components/ImageViewerModal.tsx) (+ `.css`) | Image counterpart of `PlaybackModal.tsx` (same overlay/nav/tag/delete cluster and prop shape): an `<img>` against `GET /files/{id}/stream`. |
| [`frontend/src/components/QuickTagAdd.tsx`](../frontend/src/components/QuickTagAdd.tsx) (+ `.css`) | Compact ad-hoc tag-add control floating over the playback/image screens; suggestions via `utils/tagSuggestions.ts` rendered as `TagBadge` chips, posts to `/api/files/{id}/tags`. Controlled/uncontrolled `open` hybrid so the parent can close it when `UserDefinedTagButton` opens. |
| [`frontend/src/components/UserDefinedTagButton.tsx`](../frontend/src/components/UserDefinedTagButton.tsx) (+ `.css`, reuses `QuickTagAdd.css` for `variant="overlay"`) | Picker for the user-defined pool: popover of existing user tags (`GET /api/tags?category=user`) plus a create/promote input, both posting `POST /files/{id}/tags/user-defined`. Same `open` hybrid as `QuickTagAdd`; `variant` switches overlay vs. panel chrome. |
| [`frontend/src/components/ConvertDirectoryDialog.tsx`](../frontend/src/components/ConvertDirectoryDialog.tsx) (+ `ConvertDialog.css`) | Directory convert dialog: profile picker, production/test radio, skip-processed; posts `POST /api/jobs/convert-directory`. |
| [`frontend/src/components/FileConvertModal.tsx`](../frontend/src/components/FileConvertModal.tsx) (shares `ConvertDialog.css`) | Single-file convert dialog (same fields as the directory one); posts `POST /api/jobs/convert-file`; calls `ensureDefaultProfile()` when no profiles exist. |
| [`frontend/src/components/FileTuneModal.tsx`](../frontend/src/components/FileTuneModal.tsx) (shares `ConvertDialog.css`) | Variant-sweep tuning dialog: pick an axis (dimension/CRF/codec), generate per-variant override rows, start a test-mode `convert-file` job with `variants`, poll its items, "save as profile" per result. |
| [`frontend/src/components/MoveFileDialog.tsx`](../frontend/src/components/MoveFileDialog.tsx) (+ `.css`, shares `ConvertDialog.css`) | Move dialog: lazily-expanding folder tree, "Move here" posts `POST /api/files/{id}/move`. (Favorites quick-move is separate — `DirectoryMoveMenu.tsx`.) |
| [`frontend/src/components/CreateFolderDialog.tsx`](../frontend/src/components/CreateFolderDialog.tsx) (shares `ConvertDialog.css`) | Name input → `POST /api/directories` with `{parent_path, name}`; maps `invalid_name`/`destination_collision` to messages. |
| [`frontend/src/components/DirectoryMoveMenu.tsx`](../frontend/src/components/DirectoryMoveMenu.tsx) (+ `.css`) | Compact anchored popover for drilling into a folder's subtree and picking a move destination; caller does the actual move. |
| [`frontend/src/components/FolderQuickActions.tsx`](../frontend/src/components/FolderQuickActions.tsx) (+ `.css`) | Quick "move this video here" row (in `PlaybackModal`/`FileInfoPanel`): one button per favorite folder (drills via `DirectoryMoveMenu`) plus a History popover (`utils/recentFolders.ts`). |
| [`frontend/src/components/PreviewDirectoryDialog.tsx`](../frontend/src/components/PreviewDirectoryDialog.tsx) (shares `ConvertDialog.css`) | Directory preview dialog (skip-processed only); posts `POST /api/jobs/preview-directory`. |
| [`frontend/src/components/TagDirectoryDialog.tsx`](../frontend/src/components/TagDirectoryDialog.tsx) (shares `ConvertDialog.css`) | Directory tag dialog (skip-processed only); posts `POST /api/jobs/tag-directory`. |
| [`frontend/src/components/TagLabModal.tsx`](../frontend/src/components/TagLabModal.tsx) (+ `.css`, shares `ConvertDialog.css`'s `--wide` variant) | Single-file AI-tagging review dialog: `POST .../tag-lab/prepare` renders the images/prompt immediately; `.../run` returns the raw reply + full provider JSON (collapsed `<details>`, also shown next to errors), usage, and editable ranked tags — nothing is written until `.../apply`. Model picker shows rating/KPI stats and an editable `$/1M` price; per-tag like/dislike posts to `/tag-lab/runs/{run_id}/feedback`. Both calls pass `timeoutMs` so the modal can never hang. |
| [`frontend/src/components/BackendStatusPanel.tsx`](../frontend/src/components/BackendStatusPanel.tsx) | Shows `/api/health` + `/api/app/info`; rendered in the empty state while no source is connected. |
| [`frontend/src/context/PreviewVisibilityContext.tsx`](../frontend/src/context/PreviewVisibilityContext.tsx) | Session-only `previewsVisible` toggle; flips `data-preview-profile` between `a`/`b` on the root element, switching which preview-style CSS filter applies grid-wide. |
| [`frontend/src/context/InterfaceSettingsContext.tsx`](../frontend/src/context/InterfaceSettingsContext.tsx) | Owns `theme`, `language`, `previewProfiles` (compiled to `--preview-filter-a/-b`), and `searchLimits`, backed by `GET/PUT /api/interface-settings`; applies a `localStorage`/locale guess on first paint, then reconciles with the backend. |
| [`frontend/src/context/SourceContext.tsx`](../frontend/src/context/SourceContext.tsx) | Holds the active source (`GET /api/source`); `refresh()`/`setSource()`. |
| [`frontend/src/context/JobsContext.tsx`](../frontend/src/context/JobsContext.tsx) | Polls `GET /api/jobs` every 1.5s app-wide; fetches items for the `activeJob` and every running/paused job (`jobItemsById`). |
| [`frontend/src/context/ConversionProfilesContext.tsx`](../frontend/src/context/ConversionProfilesContext.tsx) | Holds `GET /api/conversion-profiles`; `ensureDefaultProfile()` creates a starter profile when the list is empty. |
| [`frontend/src/context/TagsContext.tsx`](../frontend/src/context/TagsContext.tsx) | Holds the full tag vocabulary (`GET /api/tags`); shared by the vocabulary editor, search autocomplete, and the info panel's tag-add. |
| [`frontend/src/hooks/useBackendStatus.ts`](../frontend/src/hooks/useBackendStatus.ts) | One-shot health/app-info fetch for `BackendStatusPanel`'s empty-state screen. |
| [`frontend/src/hooks/useBackendHealth.ts`](../frontend/src/hooks/useBackendHealth.ts) | Polls `/api/health` every 5s → `'checking' \| 'online' \| 'slow' \| 'offline'`: a timeout goes `'slow'` and gets one longer retry before `'offline'`; a hard failure goes straight to `'offline'`. Timeout params default to `DEFAULT_HEALTH_TIMEOUTS` (5s/10s); self-scheduling `setTimeout` loop so the slow-path retry never overlaps the next poll. Tested in `useBackendHealth.test.ts`. |
| [`frontend/src/hooks/useBackendHealthSettings.ts`](../frontend/src/hooks/useBackendHealthSettings.ts) | One-shot `GET /api/backend-health-settings`, converting seconds to ms for `useBackendHealth.ts`; falls back to `DEFAULT_HEALTH_TIMEOUTS`. |
| [`frontend/src/hooks/useBackendStats.ts`](../frontend/src/hooks/useBackendStats.ts) | Polls `/api/system/stats` every 5s, only while `useBackendHealth` reports online; derives `networkBytesPerSec` via `utils/backendStats.ts`'s `computeNetworkRate()`. |
| [`frontend/src/themeIcons.ts`](../frontend/src/themeIcons.ts) | `THEME_ICON` map shared by `TopBar.tsx`'s cycle toggle and `InterfaceSection.tsx`'s picker. |
| [`frontend/src/utils/fetchWithTimeout.ts`](../frontend/src/utils/fetchWithTimeout.ts) | `fetchWithTimeout(url, options, timeoutMs)` — `AbortController`-based fetch ceiling; backs `api/client.ts`'s `timeoutMs` option. Tested in `fetchWithTimeout.test.ts`. |
| [`frontend/src/utils/format.ts`](../frontend/src/utils/format.ts) | `formatSize()`, `formatBitrate()`, `formatDuration()`, `formatElapsed()` (locale-agnostic, unit labels passed in). Tested in `format.test.ts`. |
| [`frontend/src/utils/backendStats.ts`](../frontend/src/utils/backendStats.ts) | `computeNetworkRate(prev, curr)` — bytes/sec between two cumulative-byte samples, clamped to 0 on the first sample, a backwards delta (backend restart), or non-finite input (stale backend with an older response shape). Tested in `backendStats.test.ts`. |
| [`frontend/src/utils/eta.ts`](../frontend/src/utils/eta.ts) | `estimateEtaSeconds()` — pure rate-extrapolation math behind `JobsModal`'s `useEtaSeconds`. Tested in `eta.test.ts`. |
| [`frontend/src/utils/layoutGeometry.ts`](../frontend/src/utils/layoutGeometry.ts) | Pure grid-geometry helpers — the client-side mirror of the backend's `compute_layout_tiles()` for instant editor feedback (backend stays canonical). Tested in `layoutGeometry.test.ts`. |
| [`frontend/src/utils/previewStyle.ts`](../frontend/src/utils/previewStyle.ts) | `PreviewStyleProfile` type, `DEFAULT_PREVIEW_PROFILE_A/_B`, `previewFilterCss()` — profile → CSS `filter` string. |
| [`frontend/src/utils/recentlyViewed.ts`](../frontend/src/utils/recentlyViewed.ts) | `localStorage` most-recent-first played-file ids (cap 3); `loadPreviewSampleFileIds()` for the Settings preview-style sample thumbnails. |
| [`frontend/src/utils/recentTags.ts`](../frontend/src/utils/recentTags.ts) | `localStorage` most-recent-first de-duped tag names (cap 10) recorded by every "+"-style tag-add control; feeds `tagSuggestions.ts`. Tested in `recentTags.test.ts`. |
| [`frontend/src/utils/tagSuggestions.ts`](../frontend/src/utils/tagSuggestions.ts) | `buildTagSuggestions(recentNames, pool, perBlockLimit=10)` — recently-added-manually tags first, then pool matches; shared by `QuickTagAdd.tsx`, `FileInfoPanel.tsx`, `TagLabModal.tsx`. Tested in `tagSuggestions.test.ts`. |
| [`frontend/src/utils/searchQuery.ts`](../frontend/src/utils/searchQuery.ts) | `parseSearchQuery()`/`formatSearchQuery()` for the `tag:`/`file:`/`path:` prefixes (RU aliases accepted) plus the shared `ActiveSearch`/`SearchScope` types. Tested in `searchQuery.test.ts`. |
| [`frontend/src/utils/sortFiles.ts`](../frontend/src/utils/sortFiles.ts) | `SortBy`/`SORT_OPTIONS`/`sortFiles()` (name/size/tags), shared by `LibraryView` and `SearchResults`. |
| [`frontend/src/utils/tagColor.ts`](../frontend/src/utils/tagColor.ts) | `hashTagColor(id)` — deterministic fallback palette, mirroring `backend/app/tags.py::resolve_tag_color()` exactly; `getContrastTextColor(hex)` — WCAG luminance → black/white text. Tested in `tagColor.test.ts`. |
| [`frontend/src/utils/recentFolders.ts`](../frontend/src/utils/recentFolders.ts) | Two independent `localStorage` histories recorded only on play/move: the view log (cap 10, `FolderQuickActions`' History) and deduped visits (cap 10, `LibraryView`'s "Recent folders"). Tested in `recentFolders.test.ts`. |
| [`frontend/src/i18n/index.ts`](../frontend/src/i18n/index.ts) | i18next setup; initial language from persisted preference/browser locale, reconciled with the backend by `InterfaceSettingsContext`. |
| [`frontend/src/i18n/locales/en.json`](../frontend/src/i18n/locales/en.json), [`ru.json`](../frontend/src/i18n/locales/ru.json) | UI strings, kept in key-set parity — enforced by [`localeParity.test.ts`](../frontend/src/i18n/localeParity.test.ts). |
| [`frontend/src/types/api.ts`](../frontend/src/types/api.ts) | TypeScript types for every API payload; also exports the runtime `THEME_PRESETS` tuple — the frontend's single source of truth for theme presets. |
| [`frontend/src/api/client.ts`](../frontend/src/api/client.ts) | The central HTTP client — every backend call goes through it (no inline `fetch('/api/...')`): `api<T>()` (throws `ApiError` on non-2xx), `tryApi<T>()` (null on any failure — polling/best-effort), `rawApi()` (raw `Response`), all with optional `timeoutMs`. `ApiError` carries `status`, the backend `detail`, and a `code` getter. Tested in `client.test.ts`. |

## Notes and conventions

- **Copy-to-clipboard buttons**: no shared utility — every instance inlines the same small pattern: `await navigator.clipboard.writeText(text)`, a `copied` boolean reset via `setTimeout(~1500ms)`, and a `Check`/`Copy` icon swap. Reuse the shape rather than introducing a hook.
- **Adding/removing a theme preset** touches all of these together: (1) `THEME_PRESETS` in `frontend/src/types/api.ts`, (2) a `[data-theme="X"]` block in `styles/theme.css`, (3) a `THEME_ICON` entry in `themeIcons.ts`, (4) `theme.<key>` labels in `en.json` + `ru.json`, (5) `THEME_PRESETS` in `backend/app/interface_settings.py`, (6) the `Literal` in `backend/app/routers/interface_settings.py` (synced by hand — no shared constant across the language boundary).
- **API calls**: always through [`frontend/src/api/client.ts`](../frontend/src/api/client.ts) (`api`/`tryApi`/`rawApi`), never inline `fetch('/api/...')`; shared server state lives in the contexts above. Pick `api()` when the caller surfaces the error (try/catch → error state), `tryApi()` when failure should be silent (polling, suggestion fetches, fire-and-forget saves).
- **Compact status/labels**: prefer an icon or emoji over a text label when space is tight (job cards, status indicators) — shape + color reads at a glance and doesn't wrap. Keep the full word reachable via `title` (hover tooltip) and `aria-label` (accessibility) on the icon element, rather than dropping the information entirely. See `JobsModal.tsx`'s `STATUS_ICON` map for the pattern.
