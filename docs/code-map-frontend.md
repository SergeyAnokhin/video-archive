# Code Map — Frontend

Part of the [code map](code-map.md). **Update this file whenever frontend files are added, moved, or removed.** Keep entries short — what lives there and when to go there; rationale and history live in git.

## `frontend/` — Vite + React + TypeScript

| Path | Role |
| --- | --- |
| [`frontend/package.json`](../frontend/package.json) | Scripts (`dev`, `build`, `lint`, `preview`, `test` — vitest) and deps (React, i18next, react-i18next, `lucide-react`). |
| [`frontend/vite.config.ts`](../frontend/vite.config.ts) | Dev server on `5173` (`PORT` override), host `0.0.0.0`; proxies `/api` to the backend on `127.0.0.1:8000` (`BACKEND_PORT` override). |
| [`frontend/index.html`](../frontend/index.html) | HTML shell; `data-theme` on the root selects the active theme variable set. |
| [`frontend/src/main.tsx`](../frontend/src/main.tsx) | Entry point; wires global styles and i18n before rendering `App`. |
| [`frontend/src/App.tsx`](../frontend/src/App.tsx) | Root component: `InterfaceSettingsProvider` (outermost) → `PreviewVisibilityProvider` → `AppLayout`. |
| [`frontend/src/styles/theme.css`](../frontend/src/styles/theme.css) | CSS variable sets for all eight theme presets (`strict`/`playful`/`casino`/`neon`/`toxic`/`cyber`/`vivid`/`mono`), including the second accent `--color-accent-2` and per-preset decorative glow/pulse rules gated behind `prefers-reduced-motion`. |
| [`frontend/src/styles/global.css`](../frontend/src/styles/global.css) | Reset, base typography/touch-target rules, and theme-following scrollbar styling (reads the theme's surface/border/text variables). |
| [`frontend/src/components/TopBar.tsx`](../frontend/src/components/TopBar.tsx) (+ `.css`) | Top bar: menu toggle (opens the directory-tree pane), title, centered `LibrarySearchBox`, then icon buttons — activity indicator (while a job runs), Jobs, preview-visibility eye toggle, theme cycle (iterates `THEME_PRESETS`), Settings. `activeSearch` state itself lives in `AppLayout.tsx`. |
| [`frontend/src/components/LibrarySearchBox.tsx`](../frontend/src/components/LibrarySearchBox.tsx) (+ `.css`) | Scoped search box: parses `tag:`/`file:`/`path:` prefixes (Russian aliases too) via `utils/searchQuery.ts`; shows up to three suggestion groups (tags client-side, fetched once from `GET /api/tags/used` — every pool, not just the AI vocabulary, user request; files/folders via debounced server queries), each capped by `searchLimits`; submits an `ActiveSearch` upward, or navigates directly when a folder suggestion is picked. |
| [`frontend/src/components/SearchResults.tsx`](../frontend/src/components/SearchResults.tsx) (uses `LibraryView.css`) | Search-results view: unscoped search renders up to three capped groups (each with a "search only in this group" button); a scoped search renders one flat infinite-scroll grid (`IntersectionObserver`, 60/page). Shares `sortFiles()` with `LibraryView.tsx`. |
| [`frontend/src/components/SettingsModal.tsx`](../frontend/src/components/SettingsModal.tsx) (+ `.css`) | Tabbed settings modal driven by a fixed `TABS` array (icon sidebar on desktop, horizontal icon strip on mobile; full-screen sheet below 640px). Tab order: source (Source + SavedSources merged), conversion profiles, preview, playback, tagging, providers, backup, performance, network, interface. Shared utilities: `.settings-modal__option(--icon)` and `.settings-modal__row`. |
| [`frontend/src/components/SourceSection.tsx`](../frontend/src/components/SourceSection.tsx) | Source connection form: `local`/`smb` protocol toggle, test-connection, connect/replace with confirmation, SMB Reconnect button, and a post-connect restore offer when `detected_backups` is non-empty. |
| [`frontend/src/components/SavedSourcesSection.tsx`](../frontend/src/components/SavedSourcesSection.tsx) | Saved-sources list (`GET /api/sources`): per row — active badge, preview-cache size/count, switch (`POST /activate`), clear-preview-cache, forget (disabled for the active source). |
| [`frontend/src/components/PlaybackSettingsSection.tsx`](../frontend/src/components/PlaybackSettingsSection.tsx) | Single `stream`/`direct_link` mode selector (`GET/PUT /api/playback-settings`). |
| [`frontend/src/components/PerformanceSettingsSection.tsx`](../frontend/src/components/PerformanceSettingsSection.tsx) | Single `parallel_workers` input (1-16, `GET/PUT /api/performance-settings`). |
| [`frontend/src/components/NetworkAccessSection.tsx`](../frontend/src/components/NetworkAccessSection.tsx) | Settings → Network: lists `http://<lan-ip>:<port>` for each detected address (`GET /api/app/network-info`) with copy buttons and a manual refresh, plus static LAN/hotspot/firewall instructions. |
| [`frontend/src/components/ConversionProfilesSection.tsx`](../frontend/src/components/ConversionProfilesSection.tsx) | Conversion profile create/edit/duplicate/delete/mark-default via `useConversionProfiles()`; includes the internal `ConversionProfileForm`. |
| [`frontend/src/components/PreviewSettingsSection.tsx`](../frontend/src/components/PreviewSettingsSection.tsx) (+ `.css`) | Preview settings, two local sub-tabs. **Collage**: aspect-ratio selector, the construction-set grid editor (tile geometry fetched from `POST /api/preview-layouts/preview` — backend stays the source of truth), preset gallery + quick save/load slots. **Animated preview**: folder-GIF frame count, GIF width/colors, source mode (`frame`/`clip`), segment duration, transition. All persisted via `/api/preview-settings`. |
| [`frontend/src/components/LayoutCanvas.tsx`](../frontend/src/components/LayoutCanvas.tsx) | Black-canvas grid renderer for the layout editor (styles in `PreviewSettingsSection.css`): renders `LayoutTile[]` at an aspect ratio, doubling as the interactive editor surface and as `mini` gallery thumbnails. |
| [`frontend/src/components/TaggingSettingsSection.tsx`](../frontend/src/components/TaggingSettingsSection.tsx) | AI vocabulary editor (chips via the shared `TagChipEditor`, `category="ai"` implicitly — see below), plus sampled-frame count, top-tag count, combine-into-collage toggle, and one `image_resolution` field shared by both modes -- the pixel size of a single frame, so the collage's total size scales with the grid (e.g. a 3x3 grid at 360px/frame sends a 1080x1080 image) and switching modes never needs a resolution recompute (`/api/tagging-settings`). A "preview what the model sees" button (`POST /api/tagging-settings/preview`) renders the current (possibly unsaved) settings against the most-recently-viewed video (`utils/recentlyViewed.ts`) and shows the real output images at native pixel size in a scrollable strip. Also renders `UserDefinedTagsSection` (its own `<section>`, sibling within this tab) below. The preview demo (collage/per-frame rendering, no-video error) is covered by `TaggingSettingsSection.test.tsx`, this repo's first jsdom/`@testing-library/react` component test. |
| [`frontend/src/components/TagChipEditor.tsx`](../frontend/src/components/TagChipEditor.tsx) | Reusable tag-chip CRUD list (deterministic per-id color via `tagColor()`; click = toggle active, double-click = rename inline, `×` = delete; header copy-all button; add-new input) extracted so `TaggingSettingsSection` (AI vocabulary) and `UserDefinedTagsSection` (user-defined pool) share one implementation -- caller owns the `tags`/`onChanged` fetch and passes `createDefaults` (`{is_ai_vocabulary, is_user_defined}`) so "add" lands in the right pool. |
| [`frontend/src/components/UserDefinedTagsSection.tsx`](../frontend/src/components/UserDefinedTagsSection.tsx) | Settings' management UI for the user-defined tag pool (user request — purely subjective, never AI-scored): its own `GET /api/tags?category=user` fetch + `TagChipEditor` with `createDefaults={{is_ai_vocabulary:false, is_user_defined:true}}`. |
| [`frontend/src/components/ProviderSettingsSection.tsx`](../frontend/src/components/ProviderSettingsSection.tsx) | Priority-ordered provider entry list: reorder arrows (`POST /reorder`), enable toggle, edit (inline `ProviderEntryForm`), extras row (`text_model`, `batch_enabled` — rendered only for `BATCH_SUPPORTED` types gemini/mistral), delete. Add flow includes a "Load models" button (`POST .../models`; disabled for `fal`) feeding a `<datalist>`. Export button downloads entries **including plaintext keys** behind a confirm. Import button (hidden `<input type=file>`) reads an export JSON back in via `POST .../import`, behind a confirm, and alerts the imported/skipped counts. `ModelPricingSection` (user request) renders an editable per-`(provider_type, model_name)` `$/1M` price table (`GET`/`PUT /api/settings/model-pricing`, source badge, updated-at) with a "Refresh from OpenRouter" button (`POST .../refresh-openrouter`) reporting matched/unmatched model strings. `ProviderUsageSection` below renders the per-(provider, model) usage table (`GET /api/settings/provider-usage`). |
| [`frontend/src/components/BackupMaintenanceSection.tsx`](../frontend/src/components/BackupMaintenanceSection.tsx) | Backup list with Restore/Delete, create-backup (`include_secrets` checkbox), retention count, and the three maintenance triggers (full rescan / cleanup / optimize DB). Watches `useJobs()` to refresh the list when its backup/restore job finishes. |
| [`frontend/src/components/InterfaceSection.tsx`](../frontend/src/components/InterfaceSection.tsx) | Language picker, 8-preset theme picker (two-dot accent swatches), search-limit inputs, and two `PreviewProfileEditor` blocks (per-profile sample thumbnails styled live via `previewFilterCss()`, six sliders with per-field reset-to-no-op buttons). All via `useInterfaceSettings()`. |
| [`frontend/src/components/JobsModal.tsx`](../frontend/src/components/JobsModal.tsx) (+ `.css`) | Jobs modal: status-grouped cards in a responsive grid; running/paused cards show progress bar (`total_items`), current item, elapsed, ETA (`useEtaSeconds` rolling window); `failed_item_count` shown red on any card. Per-card cancel/pause/resume/restart/remove (pause gated by `PAUSABLE_JOB_TYPES`), clear-finished, view-log (opens `LogViewerModal` filtered to the job), and a header button opening `BatchSubmissionsModal`. |
| [`frontend/src/components/BatchSubmissionsModal.tsx`](../frontend/src/components/BatchSubmissionsModal.tsx) (+ `.css`) | Read-only list of still-polling batch submissions (`GET /api/jobs/batch-submissions`, refreshed every 5s) with a per-row "forget locally" action (no provider-side cancel). |
| [`frontend/src/components/LogViewerModal.tsx`](../frontend/src/components/LogViewerModal.tsx) (+ `.css`) | Log viewer: job/file/level filters, backfill from `GET /api/logs` + live `EventSource` on `/api/logs/stream`, capped at 500 events, copy-all button. Rows show elapsed `MM:SS` timestamps and a clickable `#N` file badge (`payload.file_index`) that toggles the file filter; the `[#N]` message prefix is stripped for display. |
| [`frontend/src/components/AppLayout.tsx`](../frontend/src/components/AppLayout.tsx) | Shell: top bar + collapsible directory-tree pane (closed by default at every width; drawer overlay below 640px) + main content + modals. Owns the selected-folder path and the `activeSearch` state shared by `TopBar` and `LibraryView`. |
| [`frontend/src/components/DirectoryTree.tsx`](../frontend/src/components/DirectoryTree.tsx) (+ `.css`) | Recursive expandable folder tree (`GET /api/tree?include_status=true`) with status dots; refetches when the active source changes. The fetched root node stands in for the active source (its name), so it's never rendered as a row -- only `root.children` down are shown. A toolbar above the list has a name filter (prunes to matching branches + their ancestors, force-expanded) and expand-all/collapse-all buttons (lift collapse state into a `Set<path>` at the tree level). |
| [`frontend/src/components/LibraryView.tsx`](../frontend/src/components/LibraryView.tsx) (+ `.css`) | The library screen: fetches `GET /api/directories/children` for the selected path (or delegates to `SearchResults.tsx` while a search is active). Toolbar: breadcrumb, create-folder, sort-cycle button (name/size/tags via `utils/sortFiles.ts`), rescan, and convert/preview/tag folder actions (collapsing into an overflow menu below 640px). Cards live in `LibraryCards.tsx`; thumbnails are animated GIFs with the grid-wide preview-style CSS filter. Reloads (`reloadTick`) on: any job item for a file in the current folder reaching `completed` (watches `useJobs()`'s `jobItemsById`, live per-item -- doesn't wait for the whole job to finish, user request), the whole job finishing (`activeJob` going null, a catch-all e.g. for folder-level results), and after delete/move/favorite/create-folder. A same-folder reload never clears `data` first (only a real path change does), so cards update their props in place with no loading-state flash. `playingFile` opens `PlaybackModal` for a video or `ImageViewerModal` for a standalone image (post-V1, user request — branches on `file.is_video_supported`, one shared open/prev/next state for both), and `recordRecentlyViewed()` (video-only "recently played" history) is skipped for images. Computes prev/next for both modals/`FileInfoPanel` from the current folder's sorted list; records recently-viewed file/folder history. A "Recent folders" button in the toolbar (next to breadcrumb) pops a menu of the last 10 distinct folders where a video was played or a file was moved to (`utils/recentFolders.ts`'s `recordFolderVisit()`/`getRecentFolderVisits()`, excluding the current folder) -- plain browsing/breadcrumb clicks don't add entries. Each entry renders as clickable path crumbs (small/muted for ancestors, normal-size for the folder itself) rather than one flat button, so picking an ancestor jumps there instead of the deepest folder. |
| [`frontend/src/components/LibraryCards.tsx`](../frontend/src/components/LibraryCards.tsx) | `FolderCard`/`FileCard` (styles in `LibraryView.css`). `FolderCard`: `folder-preview.gif` thumb, status dots, aggregate size/tags row, favorite star + delete button (card itself is a `div[role="button"]`). `FileCard`: click-to-play GIF thumb (falls back to `preview.jpg` on error) for video, or the file's own bytes via `GET /files/{id}/stream` for a standalone image (post-V1, user request — `is_image_supported`, `ImageIcon` fallback on load error), "i" info button, duration badge, status dots (conversion/preview dots stay video-only), variant/original markers, bottom-left tuning-parameter caption overlay, up to 4 AI-tag chips (score/model in the `title` tooltip), delete button for variants. |
| [`frontend/src/components/FileInfoPanel.tsx`](../frontend/src/components/FileInfoPanel.tsx) (+ `.css`, shares `ConvertDialog.css`) | Near-fullscreen per-file panel: still JPEG collage (or the image itself via `GET /files/{id}/stream` for a standalone image) + details column (status pills, tuning-parameter chips, editable AI-tags section — per-tag remove, add via `<datalist>` autocomplete (sourced from `GET /api/tags/used`, every pool) or free text creating an ad-hoc tag (no managed pool, user request), "detected by" model line, `UserDefinedTagButton` (`variant="panel"`) for the user-defined pool — and a media-info grid fetched from `GET /api/files/{id}/media-info`, ffprobe-backed so it still returns size/resolution/format for a plain image). One plain button per action: preview/convert/tune only for `is_video_supported` files (post-V1, user request — meaningless for a standalone image), tag/similar/move/delete always; "tag" opens `TagLabModal` (closing this panel first, same convention as convert/tune/similar/move) rather than posting a background job. Optional prev/next nav and `FolderQuickActions`; `onTagsChanged` lets `LibraryView` refetch on close. The collage/details split is a draggable divider (`--fip-split` CSS var, pointer + arrow-key resize, 25–85% clamped) persisted to `localStorage['video-archive:file-info-split']`; hidden below 640px where the layout stacks. |
| [`frontend/src/components/SimilarFilesModal.tsx`](../frontend/src/components/SimilarFilesModal.tsx) (+ `.css`, shares `ConvertDialog.css`) | Lists `GET /api/files/{id}/similar` matches (path + bit difference); empty list is a normal outcome. Works for standalone images too (post-V1, user request — same-kind-only matches, backend-enforced) with no component change. |
| [`frontend/src/components/PlaybackModal.tsx`](../frontend/src/components/PlaybackModal.tsx) (+ `.css`) | Full-viewport playback overlay: HTML5 `<video>` against the stream URL, or the copyable direct local/UNC path plus a clickable raw-stream link (`info.stream_url`, opens `target="_blank"` — the bare backend stream response, no app UI) in `direct_link` mode; mode-switch and close buttons float over the video; Escape/backdrop closes. Optional prev/next chevrons + arrow-key handlers and `FolderQuickActions`. Bottom-left quick-actions group: an `onOpenInfo` button (wired by `LibraryView` to switch straight into `FileInfoPanel` for the same file, closing playback), `QuickTagAdd`, and `UserDefinedTagButton`. Only ever rendered for `is_video_supported` files — `ImageViewerModal` is `LibraryView`'s counterpart for standalone images. |
| [`frontend/src/components/ImageViewerModal.tsx`](../frontend/src/components/ImageViewerModal.tsx) (+ `.css`) | Full-viewport image viewer for a standalone image (post-V1, user request), modeled on `PlaybackModal.tsx`: same overlay/close/Escape/backdrop-click, prev/next nav zones + arrow-key handlers, top-left `FolderQuickActions` + `QuickTagAdd` + `UserDefinedTagButton` + info-toggle cluster — just an `<img>` against `GET /files/{id}/stream` instead of playback-mode UI (no stream-vs-direct-link concept for a static image). Same prop shape as `PlaybackModal` so `LibraryView` wires both the same way. |
| [`frontend/src/components/QuickTagAdd.tsx`](../frontend/src/components/QuickTagAdd.tsx) (+ `.css`) | Compact toggle-open tag-add control meant to float directly over the playback screen's video (no modal, transparent/blurred background). Empty input suggests the 5 most-recently-used tags (`utils/recentTags.ts`); typing debounce-fetches `GET /api/tags/used` (every tag pool, usage-ordered) for prefix matches. Posts to `/api/files/{id}/tags` same as `FileInfoPanel`'s tag-add -- an ad-hoc tag, no managed pool. |
| [`frontend/src/components/UserDefinedTagButton.tsx`](../frontend/src/components/UserDefinedTagButton.tsx) (+ `.css`, reuses `QuickTagAdd.css` for its `variant="overlay"` chrome) | Dedicated picker for the user-defined tag pool (user request), distinct from the free-text add controls above: toggle-open popover lists existing user-defined tags (`GET /api/tags?category=user`) as one-click picks, plus a text input to create (or promote) a new one on the fly -- both post to `POST /files/{id}/tags/user-defined`. `variant` switches chrome between floating-over-video (`PlaybackModal`/`ImageViewerModal`) and an opaque popover (`FileInfoPanel`, `variant="panel"`, own CSS). |
| [`frontend/src/components/ConvertDirectoryDialog.tsx`](../frontend/src/components/ConvertDirectoryDialog.tsx) (+ `ConvertDialog.css`) | Directory convert dialog: profile picker, production/test radio, skip-processed; posts `POST /api/jobs/convert-directory`. |
| [`frontend/src/components/FileConvertModal.tsx`](../frontend/src/components/FileConvertModal.tsx) (shares `ConvertDialog.css`) | Single-file convert dialog (same fields as the directory one); posts `POST /api/jobs/convert-file`; calls `ensureDefaultProfile()` when no profiles exist. |
| [`frontend/src/components/FileTuneModal.tsx`](../frontend/src/components/FileTuneModal.tsx) (shares `ConvertDialog.css`) | Variant-sweep tuning dialog: pick an axis (dimension/CRF/codec), generate per-variant override rows, start a test-mode `convert-file` job with `variants`, poll its items, and "save as profile" per result. CRF uses a min/max/step range; codec and dimension (`DIMENSION_PRESETS` = 480/640/720/960/1024px) use a checkbox list instead. |
| [`frontend/src/components/MoveFileDialog.tsx`](../frontend/src/components/MoveFileDialog.tsx) (+ `.css`, shares `ConvertDialog.css`) | Move dialog: lazily-expanding folder tree from the source root, "Move here" posts `POST /api/files/{id}/move`. (The favorites quick-move flow is separate — `DirectoryMoveMenu.tsx`.) |
| [`frontend/src/components/CreateFolderDialog.tsx`](../frontend/src/components/CreateFolderDialog.tsx) (shares `ConvertDialog.css`) | Name input → `POST /api/directories` with `{parent_path, name}`; maps `invalid_name`/`destination_collision` to messages. |
| [`frontend/src/components/DirectoryMoveMenu.tsx`](../frontend/src/components/DirectoryMoveMenu.tsx) (+ `.css`) | Compact anchored popover for drilling into one folder's subtree level-by-level and picking a move destination; caller does the actual move. |
| [`frontend/src/components/FolderQuickActions.tsx`](../frontend/src/components/FolderQuickActions.tsx) (+ `.css`) | Quick "move this video here" button row (mounted in `PlaybackModal`/`FileInfoPanel`): one button per favorite folder (drills via `DirectoryMoveMenu` when it has subfolders) plus a History popover of recently-viewed-from folders (`utils/recentFolders.ts`). |
| [`frontend/src/components/PreviewDirectoryDialog.tsx`](../frontend/src/components/PreviewDirectoryDialog.tsx) (shares `ConvertDialog.css`) | Directory preview dialog (skip-processed only); posts `POST /api/jobs/preview-directory`. |
| [`frontend/src/components/TagDirectoryDialog.tsx`](../frontend/src/components/TagDirectoryDialog.tsx) (shares `ConvertDialog.css`) | Directory tag dialog (skip-processed only); posts `POST /api/jobs/tag-directory`. |
| [`frontend/src/components/TagLabModal.tsx`](../frontend/src/components/TagLabModal.tsx) (+ `.css`, shares `ConvertDialog.css`'s `--wide` variant) | Single-file AI-tagging review dialog (user request — compare provider entries/models one at a time, no job queue, no batch API): pick an enabled provider entry, `POST /files/{id}/tag-lab/run` returns a `run_id`, the images sent (click any to zoom into a fullscreen overlay), the prompt (collapsed `<details>`), the model's full raw reply (collapsed `<details>`) with token/cost usage, and every vocabulary tag ranked by score — nothing is written yet. The model picker also shows a compact like-ratio/apply-KPI stats line (`GET /api/settings/model-ratings`, hover tooltips) for the selected entry's `(provider_type, model_name)`. The suggested tags (`score > 0`) seed an editable local list (remove any, add more via the same all-pools `<datalist>`/free-text pattern as `FileInfoPanel`'s tag-add, sourced from `GET /api/tags/used`); model-sourced tags get like/dislike buttons (`POST /tag-lab/runs/{run_id}/feedback`, click-again clears). Below the usage line, an editable `$/1M in/out` price (`GET`/`PUT /api/settings/model-pricing`, inline pencil-to-edit, source badge). "Apply" posts `POST /files/{id}/tag-lab/apply` with `run_id` (a manually-typed addition here is an ordinary ad-hoc tag, not AI vocabulary, and never gets vote buttons — only ever counts toward the apply-KPI's "changed" bucket), "Cancel" just closes (nothing was ever written). |
| [`frontend/src/components/BackendStatusPanel.tsx`](../frontend/src/components/BackendStatusPanel.tsx) | Shows `/api/health` + `/api/app/info`; rendered in the empty state while no source is connected. |
| [`frontend/src/context/PreviewVisibilityContext.tsx`](../frontend/src/context/PreviewVisibilityContext.tsx) | Session-only `previewsVisible` toggle; flips `data-preview-profile` between `a`/`b` on the root element, switching which preview-style CSS filter applies grid-wide (nothing is actually hidden). |
| [`frontend/src/context/InterfaceSettingsContext.tsx`](../frontend/src/context/InterfaceSettingsContext.tsx) | Owns `theme`, `language`, `previewProfiles` (`{a, b}` — compiled to `--preview-filter-a/-b` via `previewFilterCss()`), and `searchLimits`, all backed by `GET/PUT /api/interface-settings`. Applies a `localStorage`/browser-locale guess synchronously on first paint, then reconciles with the backend. Setters update local state, i18next, `localStorage`, and the backend in one call. |
| [`frontend/src/context/SourceContext.tsx`](../frontend/src/context/SourceContext.tsx) | Holds the active source (`GET /api/source`); `refresh()`/`setSource()`. |
| [`frontend/src/context/JobsContext.tsx`](../frontend/src/context/JobsContext.tsx) | Polls `GET /api/jobs` every 1.5s app-wide; fetches items for the single `activeJob` (top-bar tooltip) and for every running/paused job (`jobItemsById`, for multi-card progress in `JobsModal`). |
| [`frontend/src/context/ConversionProfilesContext.tsx`](../frontend/src/context/ConversionProfilesContext.tsx) | Holds `GET /api/conversion-profiles`; `ensureDefaultProfile()` creates a starter profile when the list is empty. |
| [`frontend/src/context/TagsContext.tsx`](../frontend/src/context/TagsContext.tsx) | Holds the full tag vocabulary (`GET /api/tags`); shared by the vocabulary editor, search autocomplete, and the info panel's tag-add `<datalist>`. |
| [`frontend/src/hooks/useBackendStatus.ts`](../frontend/src/hooks/useBackendStatus.ts) | Health/app-info fetch with loading/error/ready state. |
| [`frontend/src/themeIcons.ts`](../frontend/src/themeIcons.ts) | `THEME_ICON` map shared by `TopBar.tsx`'s cycle toggle and `InterfaceSection.tsx`'s picker. |
| [`frontend/src/utils/format.ts`](../frontend/src/utils/format.ts) | `formatSize()`, `formatBitrate()`, `formatDuration()`, `formatElapsed()` (locale-agnostic, unit labels passed in). Tested in `format.test.ts`. |
| [`frontend/src/utils/eta.ts`](../frontend/src/utils/eta.ts) | `estimateEtaSeconds()` — pure rate-extrapolation math behind `JobsModal`'s `useEtaSeconds`. Tested in `eta.test.ts`. |
| [`frontend/src/utils/layoutGeometry.ts`](../frontend/src/utils/layoutGeometry.ts) | Pure grid-geometry helpers — the client-side mirror of the backend's `compute_layout_tiles()` for instant editor feedback (backend stays canonical). Tested in `layoutGeometry.test.ts`. |
| [`frontend/src/utils/previewStyle.ts`](../frontend/src/utils/previewStyle.ts) | `PreviewStyleProfile` type, `DEFAULT_PREVIEW_PROFILE_A/_B`, `previewFilterCss()` — profile → CSS `filter` string. |
| [`frontend/src/utils/recentlyViewed.ts`](../frontend/src/utils/recentlyViewed.ts) | `localStorage` most-recent-first played-file ids (cap 3); `loadPreviewSampleFileIds()` for the Settings preview-style sample thumbnails (falls back to the first previewed videos). |
| [`frontend/src/utils/recentTags.ts`](../frontend/src/utils/recentTags.ts) | `localStorage` most-recent-first, case-insensitively de-duped tag display names (cap 5) recorded by `QuickTagAdd.tsx`; feeds its empty-input suggestion list. Tested in `recentTags.test.ts` (same in-memory `localStorage` stub as `recentFolders.test.ts`). |
| [`frontend/src/utils/searchQuery.ts`](../frontend/src/utils/searchQuery.ts) | `parseSearchQuery()`/`formatSearchQuery()` for the `tag:`/`file:`/`path:` prefixes (RU aliases accepted, EN canonical) plus the shared `ActiveSearch`/`SearchScope` types. Tested in `searchQuery.test.ts`. |
| [`frontend/src/utils/sortFiles.ts`](../frontend/src/utils/sortFiles.ts) | `SortBy`/`SORT_OPTIONS`/`sortFiles()` (name/size/tags), shared by `LibraryView` and `SearchResults`. |
| [`frontend/src/utils/recentFolders.ts`](../frontend/src/utils/recentFolders.ts) | Two independent `localStorage` histories, both recorded only on play/move (never on plain browsing): `recordRecentFolder`/`getRecentFolders` is a view log of folders (cap 10, no consecutive duplicates) feeding `FolderQuickActions`' History button (called from `LibraryView.tsx`'s file-play handler); `recordFolderVisit`/`getRecentFolderVisits` is a deduped, most-recent-first list (cap 10) of folders where a video was played (`LibraryView.tsx`'s grid file-play handler) or a file was moved to (`FolderQuickActions.tsx`'s `moveTo()`, `MoveFileDialog.tsx`'s `handleMove()`), feeding `LibraryView.tsx`'s "Recent folders" quick-jump button. Tested in `recentFolders.test.ts` (in-memory `localStorage` stub, predates jsdom being available — see `development.md`'s test-suite table for the per-file `@vitest-environment jsdom` + `@testing-library/react` pattern now available for component tests). |
| [`frontend/src/i18n/index.ts`](../frontend/src/i18n/index.ts) | i18next setup; initial language from persisted preference/browser locale, reconciled with the backend by `InterfaceSettingsContext`. |
| [`frontend/src/i18n/locales/en.json`](../frontend/src/i18n/locales/en.json), [`ru.json`](../frontend/src/i18n/locales/ru.json) | UI strings, kept in key-set parity — enforced by [`localeParity.test.ts`](../frontend/src/i18n/localeParity.test.ts). |
| [`frontend/src/types/api.ts`](../frontend/src/types/api.ts) | TypeScript types for every API payload (source, tree, files, jobs, conversion, preview, tagging, playback, backup, interface/search settings, similarity). Also exports the runtime `THEME_PRESETS` tuple — the frontend's single source of truth for theme presets. |

## Notes and conventions

- **Copy-to-clipboard buttons**: no shared utility — every instance inlines the same small pattern: `await navigator.clipboard.writeText(text)`, a `copied` boolean reset via `setTimeout(~1500ms)`, and a `Check`/`Copy` icon swap. Reuse the shape rather than introducing a hook.
- **Adding/removing a theme preset** touches all of these together: (1) `THEME_PRESETS` in `frontend/src/types/api.ts`, (2) a `[data-theme="X"]` block in `styles/theme.css`, (3) a `THEME_ICON` entry in `themeIcons.ts`, (4) `theme.<key>` labels in `en.json` + `ru.json`, (5) `THEME_PRESETS` in `backend/app/interface_settings.py`, (6) the `Literal` in `backend/app/routers/interface_settings.py` (synced by hand — no shared constant across the language boundary).
- **No API-client module**: components call `fetch('/api/...')` directly; shared server state lives in the contexts above.
- **Compact status/labels**: prefer an icon or emoji over a text label when space is tight (job cards, status indicators) — shape + color reads at a glance and doesn't wrap. Keep the full word reachable via `title` (hover tooltip) and `aria-label` (accessibility) on the icon element, rather than dropping the information entirely. See `JobsModal.tsx`'s `STATUS_ICON` map and its running-job meta row (`Timer`/`Hourglass` icons with `formatElapsedCompact` text, full sentence in `title`) for the pattern.
