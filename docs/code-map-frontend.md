# Code Map — Frontend

Part of the [code map](code-map.md). **Update this file whenever frontend files are added, moved, or removed.**

## `frontend/` — Vite + React + TypeScript

| Path | Role |
| --- | --- |
| [`frontend/package.json`](../frontend/package.json) | Frontend scripts (`dev`, `build`, `lint`, `preview`, `test` — vitest, see [development.md](development.md#tests)) and dependencies (React, i18next, react-i18next, `lucide-react`). |
| [`frontend/vite.config.ts`](../frontend/vite.config.ts) | Dev server on `127.0.0.1:5173`; proxies `/api` to the backend on `127.0.0.1:8000`. |
| [`frontend/index.html`](../frontend/index.html) | HTML shell; `data-theme="strict"` selects the active theme variable set. |
| [`frontend/src/main.tsx`](../frontend/src/main.tsx) | Entry point; wires up global styles and i18n before rendering `App`. |
| [`frontend/src/App.tsx`](../frontend/src/App.tsx) | Root component: `InterfaceSettingsProvider` (outermost, [Stage 9](./spec/roadmap.md#stage-9--polish-and-optional-features)) wrapping `PreviewVisibilityProvider` wrapping `AppLayout` wrapping the (currently placeholder) main content. |
| [`frontend/src/styles/theme.css`](../frontend/src/styles/theme.css) | CSS variables for the Strict theme (`[data-theme="strict"]`) and, since [Stage 9](./spec/roadmap.md#stage-9--polish-and-optional-features), Playful (`[data-theme="playful"]`, [Design System §2.2](./spec/design-system.md#22-playful)): a warmer/more saturated accent on the same dark neutral structure, plus playful-only decorative rules (hover lift on every button, a glow + slow pulse on primary-action buttons via the `[class$="--primary"]` BEM-suffix convention those buttons already share) gated behind `@media (prefers-reduced-motion: no-preference)` and never present under Strict. A third preset, Casino (`[data-theme="casino"]`, user request), reuses the same structure with a louder neon-on-near-black palette (magenta/gold/green accents), a static neon-outline glow on top bar/cards/dialogs, and its own hover-pulse animations — same `prefers-reduced-motion` gating as Playful. |
| [`frontend/src/styles/global.css`](../frontend/src/styles/global.css) | Reset and base typography/touch-target rules. |
| [`frontend/src/components/TopBar.tsx`](../frontend/src/components/TopBar.tsx) (+ `.css`) | Top bar, redesigned to consolidate global controls in one row (user request): menu toggle (opens the directory-tree pane at every breakpoint, not just mobile/tablet — the tree is collapsed by default everywhere now, see `AppLayout.tsx`), app title, a centered `LibrarySearchBox` (moved here from `LibraryView.tsx` so search lives in the header rather than the per-folder toolbar), then a right-hand icon-button group: an activity indicator (only rendered while a job is queued/running, hover tooltip via `jobs.indicatorTooltip`, spins with a functional CSS animation), a persistent Jobs button, preview-visibility toggle (eye icon), a theme toggle (single icon-only button cycling `strict → playful → casino → strict` via `useInterfaceSettings()`, icon and label follow the *next* theme in the cycle), and the Settings button. `activeSearch` state itself lives in `AppLayout.tsx` (passed down as `onSearch`/`onClearSearch`) since both `TopBar` (renders the box) and `LibraryView` (renders results) need it. Both Jobs-related buttons open `JobsModal`. Language selection is not in the top bar — see `SettingsModal.tsx`. |
| [`frontend/src/components/LibrarySearchBox.tsx`](../frontend/src/components/LibrarySearchBox.tsx) (+ `.css`) | Tag/name search box, extracted from `LibraryView.tsx` so `TopBar.tsx` can render it too: filters `useTags()`'s already-loaded vocabulary by prefix as the user types (no per-keystroke request) and, on picking a suggestion or pressing Enter, calls `onSearch` with an `ActiveSearch` (`{ kind: 'tag' \| 'name', label, value }`) that the parent (`AppLayout.tsx`) turns into `LibraryView`'s flat search-results view. |
| [`frontend/src/components/SettingsModal.tsx`](../frontend/src/components/SettingsModal.tsx) (+ `.css`) | Tabbed settings surface opened from the top bar's Settings button (user request, replacing an earlier single long-scrolling stack of sections): a fixed `TABS` array (id/icon/i18n label key/section component) drives an icon+label sidebar (desktop, `.settings-modal__tabs`) that becomes a horizontal icon-only scrollable strip on mobile (`.settings-modal__tab-label` hidden below 640px, `title`/`aria-label` still carry the text); only the active tab's section component is mounted, each rendered inside a single scrolling `.settings-modal__content` pane so nested sections never introduce their own scrollbars. Tab order: `SourceSection`, `ConversionProfilesSection`, `PreviewSettingsSection`, `PlaybackSettingsSection`, `TaggingSettingsSection`, `ProviderSettingsSection`, `BackupMaintenanceSection`, `InterfaceSection`. Becomes a full-screen sheet on mobile ([Design System §5](./spec/design-system.md#5-responsive-breakpoints)), with tab switching still available via the icon strip rather than one long page. Shared `SettingsModal.css` utilities used across section components: `.settings-modal__option`/`.settings-modal__option--icon` (icon-only action buttons, e.g. delete/duplicate/refresh — self-explanatory actions drop their text label, per user request) and `.settings-modal__row` (CSS grid, `auto-fit`/`minmax(110px, 1fr)`, groups short numeric/select fields side by side instead of stacking them full-width). |
| [`frontend/src/components/SourceSection.tsx`](../frontend/src/components/SourceSection.tsx) | Source settings group: a protocol selector (`local`/`smb`) toggling between a local-path form and an SMB form (host/port/share-path/username/password), test-connection, connect/replace with a destructive-replace confirmation step per [Specification §5.2](./spec/specification.md#52-source-switching), a Reconnect button shown only for an active `smb` source ([Stage 7](./spec/roadmap.md#stage-7--smb-source-and-playback)) posting `POST /api/source/reconnect`, and ([Stage 8](./spec/roadmap.md#stage-8--backup-restore-maintenance-source-switching)) a post-connect backup-restore offer: `PUT /api/source`'s response includes `detected_backups` (backups found in the newly connected source's technical folder), rendered as a list with a "Restore" button per entry that posts `POST /api/backups/restore` and refreshes `JobsContext`. |
| [`frontend/src/components/PlaybackSettingsSection.tsx`](../frontend/src/components/PlaybackSettingsSection.tsx) | Playback settings group ([Settings §4](./spec/settings-spec.md#4-playback-settings), [UI Screens §8](./spec/ui-screens.md#8-playback-settings-screen)): a single `stream`/`direct_link` mode selector persisted via `GET/PUT /api/playback-settings`; both strategies stay available from the library regardless of this default. |
| [`frontend/src/components/ConversionProfilesSection.tsx`](../frontend/src/components/ConversionProfilesSection.tsx) | Conversion Profiles settings group ([UI Screens §6](./spec/ui-screens.md#6-conversion-profiles-screen)): create/edit/duplicate/delete/mark-default, backed by `useConversionProfiles()` and the `/api/conversion-profiles` endpoints; includes the internal `ConversionProfileForm` used for add/edit. |
| [`frontend/src/components/PreviewSettingsSection.tsx`](../frontend/src/components/PreviewSettingsSection.tsx) (+ `.css`) | Preview settings group ([UI Screens §5](./spec/ui-screens.md#5-preview-settings-screen), [Settings §3](./spec/settings-spec.md#3-preview-settings)): aspect ratio selector (`standard`/`phone-portrait`/`phone-landscape`/`ultra-wide`/`custom`, `phone-landscape` = 19.5:9 is the backend default, user request) + folder-preview frame count + GIF preview max width/color count (`gif_max_width`/`gif_colors`, user request — keeps the hover-preview GIFs small regardless of the JPEG collage's own size) (persisted via `/api/preview-settings`, applied immediately); a construction-set grid editor (small/enlarged-2×2/enlarged-3×3 brushes, fill all/clear all) whose canonical tile geometry is fetched live from `POST /api/preview-layouts/preview` (single source of truth shared with the backend renderer) and rendered on a black `LayoutCanvas` (own file, see below) with a caption bar, doubling as both editor and live preview; a built-in + custom preset gallery (mini `LayoutCanvas` thumbnails, geometry computed client-side via `utils/layoutGeometry.ts`), timeline flow, and identity-diversity toggle, all backed by `/api/preview-layouts` CRUD. Quick save/load slots are not a separate concept: they're just regular (non-builtin) presets with the fixed names `Quick 1`/`Quick 2`/`Quick 3`, created-or-updated by name — no schema or endpoint changes needed to add more slots, just more fixed names on the frontend. |
| [`frontend/src/components/LayoutCanvas.tsx`](../frontend/src/components/LayoutCanvas.tsx) | The black-canvas grid renderer for the preview layout editor, extracted from `PreviewSettingsSection.tsx` (post-V1 file split; styles stay in `PreviewSettingsSection.css`, which this file also imports): renders `LayoutTile[]` as CSS-grid tiles at a given aspect ratio, doubling as the interactive construction-set surface (`onCellClick`) and as a static `mini` preset-gallery thumbnail. |
| [`frontend/src/components/TaggingSettingsSection.tsx`](../frontend/src/components/TaggingSettingsSection.tsx) | Tagging settings group ([Settings §5](./spec/settings-spec.md#5-tagging-settings), [UI Screens §7](./spec/ui-screens.md#7-tagging-settings-screen)): tag vocabulary rendered as a wrapping row of compact colored chip badges (`.tag-chip`, user request — was one full-width row per tag) instead of a list, backed by `useTags()` and `/api/tags`. Each tag's color is a deterministic hash of its id (`tagColor()`, a small string hash into a fixed 12-color palette) so the same tag always gets the same color and adjacent tags stay visually distinct; inactive tags render dimmed with a strikethrough label instead of a separate "Deactivate" button. Interactions live on the chip itself: click the label to toggle active/inactive, double-click to rename inline (swaps in a small text input, same `PUT /api/tags/{id}` as before), click the small `×` to delete. A header "copy all tags" icon button (`navigator.clipboard.writeText`, comma-joined `display_name`s, briefly swaps its icon to a checkmark) covers copying the whole vocabulary as text for pasting elsewhere. Below the chips: sampled-frame count + top-tag count grouped in a `.settings-modal__row`, combine-into-collage toggle (persisted via `/api/tagging-settings`, applied immediately like preview settings). No default-provider/vision-model picker anymore (post-V1, moved to `ProviderSettingsSection.tsx`'s priority-ordered entry list — the tag job now resolves the active provider automatically). |
| [`frontend/src/components/ProviderSettingsSection.tsx`](../frontend/src/components/ProviderSettingsSection.tsx) | AI provider settings group (post-V1 redesign — [Settings §6](./spec/settings-spec.md#6-ai-provider-settings), [UI Screens §10](./spec/ui-screens.md#10-provider-settings-screen)): a priority-ordered list of user-managed `ProviderEntry` rows (`.provider-entry-row`, any number, including multiple entries of the same provider type) instead of one fixed card per provider type. Each row shows `{provider}/{model} […key suffix]`, up/down reorder arrows (swap client-side then `POST /provider-entries/reorder` with the full new id order — first enabled entry is tried first by the tag job's fallback chain), an enabled toggle, pencil (opens `ProviderEntryForm` inline at the bottom of the list for name/model/key-rotate), gear (toggles a lightweight inline row for the lower-priority `text_model`/`batch_enabled` fields, blur/change-to-save), and trash (confirm, then `DELETE`). "+ Add provider" opens the same `ProviderEntryForm` with no initial entry: provider-type select, API key input, a "Load models" button (disabled for `fal`, which has no model-listing API) that calls `POST /provider-entries/models` (draft key, before saving) or `POST /provider-entries/{id}/models` (editing — resolves the already-stored key server-side, so the user never has to retype it just to refresh the model list) and populates a `<datalist>` the model `<input>` autocompletes against, and a name field defaulting to `"{type}/{model}"` if left blank. An export button downloads `GET /provider-entries/export` as a file via a `Blob`/object-URL anchor click, gated by a `window.confirm()` warning that the file contains plaintext API keys (an explicit, informed exception to this app's usual "keys never leave the backend" rule). |
| [`frontend/src/components/BackupMaintenanceSection.tsx`](../frontend/src/components/BackupMaintenanceSection.tsx) ([Stage 8](./spec/roadmap.md#stage-8--backup-restore-maintenance-source-switching), [UI Screens §11](./spec/ui-screens.md#11-backup-and-maintenance-screen), [Settings §7-8](./spec/settings-spec.md#7-backup-settings)) | Backup & Maintenance settings group: backup list (`GET /api/backups`) with per-entry Restore/Delete, a "create backup" action (`include_secrets` checkbox, `POST /api/backups`) and retention count (`GET/PUT /api/backup-settings`), plus three maintenance action buttons (`POST /api/jobs/rescan-directory` with an empty path for a full rescan, `POST /api/jobs/cleanup-stale-records`, `POST /api/jobs/optimize-database`). Backup/restore both create jobs, so the section watches `useJobs()`'s polled `jobs` list for the job id it just started and auto-refreshes the backup list once that job leaves `queued`/`running`, instead of polling the list itself. |
| [`frontend/src/components/InterfaceSection.tsx`](../frontend/src/components/InterfaceSection.tsx) | Interface settings group: language picker and, since [Stage 9](./spec/roadmap.md#stage-9--polish-and-optional-features), a Strict/Playful/Casino theme picker (same button-group pattern), both backed by `useInterfaceSettings()` (`InterfaceSettingsContext.tsx`) instead of talking to `i18next`/`localStorage` directly. |
| [`frontend/src/components/JobsModal.tsx`](../frontend/src/components/JobsModal.tsx) (+ `.css`) | Jobs modal ([UI Screens §3](./spec/ui-screens.md#3-jobs-modal)): jobs grouped into running/queued/failed/cancelled/completed sections from `useJobs()`; per-row cancel/restart/remove actions (shown only when valid for the job's status) plus a "clear finished" footer action and a per-row "view log" button that opens `LogViewerModal` filtered to that job. |
| [`frontend/src/components/LogViewerModal.tsx`](../frontend/src/components/LogViewerModal.tsx) (+ `.css`) | Log viewer ([UI Screens §4](./spec/ui-screens.md#4-log-viewer)): job/file/level filter controls, backfills from `GET /api/logs` then appends events from an `EventSource` on `GET /api/logs/stream`, auto-scrolling, capped at the last 500 events client-side. |
| [`frontend/src/components/AppLayout.tsx`](../frontend/src/components/AppLayout.tsx) | Shell layout: top bar + directory-tree pane (`DirectoryTree`, or a "configure a source" placeholder) + main content (`LibraryView`, or an empty state with `BackendStatusPanel`) + `SettingsModal` + `JobsModal`. Owns the selected-folder path *and* the `activeSearch` state (an `ActiveSearch \| null` from `LibrarySearchBox.tsx`) shared between `TopBar` (renders the search box, calls `onSearch`/`onClearSearch`) and `LibraryView` (renders the results). The tree pane (`.app-nav`) is a collapsible inline panel closed by default at every breakpoint (user request — previously it was a persistent always-open pane on desktop, `>=1024px`); it only expands via the top bar's menu toggle, same as the tablet behavior before this change. On mobile (`<640px`) it's still a fixed-position drawer overlay with a backdrop. |
| [`frontend/src/components/DirectoryTree.tsx`](../frontend/src/components/DirectoryTree.tsx) (+ `.css`) | Fetches `GET /api/tree?include_status=true` and renders the recursive, expandable folder tree with conversion/preview indicator dots. Refetches whenever the active source changes. |
| [`frontend/src/components/LibraryView.tsx`](../frontend/src/components/LibraryView.tsx) (+ `.css`) | Fetches `GET /api/directories/children` (server-side filtered to `is_video_supported` files only, so only folders and videos ever appear) for the selected path or, while `activeSearch` (prop, owned by `AppLayout.tsx`) is set, a flat search-results file list from `GET /api/files?tags=`/`?search=` across the whole source. Renders a toolbar (breadcrumb + "rescan" icon button always inline, plus "convert folder"/"generate previews for folder"/"tag folder" — the search box itself now lives in `TopBar.tsx`) — the icon buttons are wrapped in a `.library-view__toolbar-actions` group (user request — they were direct flex children of the toolbar, so `justify-content: space-between` spread gaps between each individual icon instead of just between the breadcrumb and the button cluster) so they sit as one right-aligned block. Below 640px the convert/preview/tag folder actions collapse into a `MoreVertical` overflow menu instead of three inline icons — both renderings map over the same `directoryActions` array, toggled via CSS (`library-view__actions-inline` hidden, `library-view__overflow` shown). Folder cards show the animated `folder-preview.gif` thumbnail (via `GET /api/directories/preview.gif?path=`, user request) and file cards show the video's animated GIF preview (via `GET /api/files/{id}/preview.gif`, user request — a lighter hover-friendly loop, as opposed to the still JPEG collage shown in `FileInfoPanel.tsx`) whenever `previewsVisible` (from `PreviewVisibilityContext`) and the has-preview flag are both true, falling back to the folder/film icon otherwise (the card components themselves live in `LibraryCards.tsx`). `.library-card__thumb` (`LibraryView.css`) uses `aspect-ratio: 19.5 / 9` (user request — matches the `phone-landscape` default preview aspect ratio, `backend/app/preview_settings.py`) instead of a fixed height, so both folder and file thumbnails are wide/short cards with `object-fit: cover` cropping whatever the source ratio is. File cards were redesigned around two actions (user request, replacing a five-icon row): clicking the thumbnail itself opens `PlaybackModal` to play immediately, and a single small "i" (`Info`) button overlaid on the thumbnail opens `FileInfoPanel` for every other per-file operation (generate preview, tag, convert, tune, similar videos, move, delete). Folder-level bulk actions ("convert folder" etc.) are unchanged, opening `ConvertDirectoryDialog`/`PreviewDirectoryDialog`/`TagDirectoryDialog`. A job-completion effect (compares `useJobs()`'s `activeJob` against a ref of its previous value; a truthy→`null` transition means a job just finished) bumps a `reloadTick` counter that's in the directory-fetch effect's dependency array, so a freshly generated preview/converted file/tag shows up in the grid without a manual reload or folder re-entry — previously nothing refetched on job completion; the same `reloadTick` bump is reused after a file delete/move (user request) since those are synchronous REST calls, not jobs. `formatSize()` moved to `frontend/src/utils/format.ts` so `FileInfoPanel.tsx` can reuse it without an import cycle. All dialogs/actions refresh `JobsContext` (`refreshJobs`) once a job is created. |
| [`frontend/src/components/LibraryCards.tsx`](../frontend/src/components/LibraryCards.tsx) | `FolderCard`/`FileCard` — the library grid card components, extracted from `LibraryView.tsx` (post-V1 file split; the `library-card__*` styles stay in `LibraryView.css`, which this file also imports). `FolderCard`: `folder-preview.gif` thumbnail (or folder icon) + incomplete-status dots. `FileCard`: click-to-play GIF thumbnail — retries with `preview.jpg` on a GIF `onError` (404) so files previewed before GIF generation existed still show a thumbnail — plus the "i" info button, `formatSize()` meta line, conversion/preview status dots, and `file.is_variant`/`file.is_original` badges (user request — API-provided booleans, see `routers/files.py`) with a small `library-card__marker` icon (`SlidersHorizontal`/`Archive`), so `.variant-*`/`.original.*` comparison artifacts are visually distinguishable in the grid. |
| [`frontend/src/components/FileInfoPanel.tsx`](../frontend/src/components/FileInfoPanel.tsx) (+ `.css`, shares `ConvertDialog.css` for chrome) | Per-file info panel opened from a `FileCard`'s "i" button (user request, replacing the five always-visible per-card icon buttons): a near-fullscreen dialog (user request — `width/height: min(…, 100vw/100vh - overlay padding)`) with a two-column body (user request — was a single tall column with a lot of empty space below the thumbnail; stacks vertically below 640px) — the still JPEG collage thumbnail (`preview.jpg`, not the grid's animated GIF) on the left, and on the right: converted/previewed/tagged status as compact pill chips (`file-info-panel__status-item`, dot + label, same lit-dot convention as `library-card__dot`/`directory-tree__dot`) where a "done" dot (`--color-success`) also gets a `box-shadow` ring (user request — make completed items visually stand out, not just a color change) and a compact `dl` grid (`file-info-panel__media-grid`, `auto-fill, minmax(150px, 1fr)`) of technical/codec fields — size, resolution, aspect ratio, codec, bitrate, duration, container, and the conversion profile actually used (user request — "optimization" details, not just file size) — fetched on open from `GET /api/files/{id}/media-info` (fades to 50% opacity while loading; unavailable fields render as `—`). One action button per operation below both columns — generate preview (`onPreview`, `POST /api/jobs/preview-file`), tag (`onTag`, `POST /api/jobs/tag-file`), convert (`onConvert`, opens `FileConvertModal`), tune (`onTune`, opens `FileTuneModal` — split from convert into its own button/modal, user request), similar videos (`onSimilar`, opens `SimilarFilesModal`), move (`onMove`, opens `MoveFileDialog.tsx`), delete (`onDelete`, gated behind a `window.confirm()` — same pattern as `BackupMaintenanceSection.tsx`'s restore confirm — then `DELETE /api/files/{id}`) — each closes this panel except preview/tag. No combobox/dropdown — every operation is a plain button, per explicit user preference. Closes via a floating top-right `X` icon button (`file-info-panel__close`, same convention as `PlaybackModal.tsx`'s close button) instead of a bottom "Close" row. |
| [`frontend/src/components/SimilarFilesModal.tsx`](../frontend/src/components/SimilarFilesModal.tsx) (+ `.css`, shares `ConvertDialog.css` for chrome) | Similar-videos modal: fetches `GET /api/files/{id}/similar` and lists matches (relative path + Hamming-distance "bit difference") or an empty-state message — a normal, non-error outcome, since this is an optional/secondary feature and most files won't have a signature until previews have been (re)generated. |
| [`frontend/src/components/PlaybackModal.tsx`](../frontend/src/components/PlaybackModal.tsx) (+ `PlaybackModal.css`) | Full-viewport playback overlay (redesigned, user request: clicking a video should just play it, with minimal chrome around the video itself rather than a titled dialog). Fetches `GET /api/files/{id}/playback` for the mode + stream URL + direct path, then either renders an HTML5 `<video>` filling the viewport (`object-fit: contain`) against the stream URL (`stream` mode) or shows the direct local/UNC path with a copy-to-clipboard button (`direct_link` mode, since browsers can't open arbitrary local/UNC paths themselves). Only a small floating close button (top-right) and mode-switch button (bottom-right) sit on top of the video; clicking the black backdrop (or Escape) closes it. No longer shares `ConvertDialog.css` — it has its own minimal `playback-overlay__*` styles instead of the shared titled-dialog chrome. |
| [`frontend/src/components/ConvertDirectoryDialog.tsx`](../frontend/src/components/ConvertDirectoryDialog.tsx) (+ `ConvertDialog.css`) | Directory-level convert dialog ([UI Screens §1](./spec/ui-screens.md#1-main-library-screen), [Specification §6.2](./spec/specification.md#62-conversion)): profile picker (from `useConversionProfiles()`), production/test mode radio, skip-processed checkbox; posts `POST /api/jobs/convert-directory`. |
| [`frontend/src/components/FileConvertModal.tsx`](../frontend/src/components/FileConvertModal.tsx) (shares `ConvertDialog.css`) | File-level convert modal (user request — split out of the former two-tab "Convert file" modal into its own dialog, paired with `FileTuneModal.tsx` below): profile picker, production/test mode radio, skip-processed checkbox; posts `POST /api/jobs/convert-file`. If `useConversionProfiles()` resolves with zero profiles, calls `ensureDefaultProfile()` on mount instead of leaving Start disabled behind a "no profiles" warning (user request — always have a starting point). |
| [`frontend/src/components/FileTuneModal.tsx`](../frontend/src/components/FileTuneModal.tsx) (shares `ConvertDialog.css`) | File-level tuning modal (user request — the former "Tuning" tab of `FileConvertModal.tsx`, now its own button/dialog opened via `FileInfoPanel`'s `onTune`) ([UI Screens §12](./spec/ui-screens.md#12-variant-comparison-flow), [Specification §8.3](./spec/specification.md#83-variant-comparison)): a sweep-range generator — pick one axis (max dimension / CRF / codec), a min/max/step range (or a codec checkbox set), "Generate" expands it into a per-row override list (individually removable), then "Start" posts `POST /api/jobs/convert-file` with `mode: "test"` and the resulting `variants` list, polling `GET /api/jobs/{id}/items` to show each variant's result with a "save as profile" action that `POST`s `/api/conversion-profiles` using the variant's effective parameters. Same zero-profiles fallback as `FileConvertModal.tsx`: calls `ensureDefaultProfile()` on mount so the sweep always has a base profile to override from. |
| [`frontend/src/components/MoveFileDialog.tsx`](../frontend/src/components/MoveFileDialog.tsx) (+ `.css`, shares `ConvertDialog.css` for chrome) | Move-file dialog (user request), opened from `FileInfoPanel.tsx`'s "Move" button: a lazily-expanding folder tree (`DirectoryTreeNode`, one `GET /api/directories/children?path=` per expand) starting at the source root, click-to-select a destination, "Move here" posts `POST /api/files/{id}/move`, mapping the backend's `same_location`/`destination_collision` error codes to translated messages. |
| [`frontend/src/components/PreviewDirectoryDialog.tsx`](../frontend/src/components/PreviewDirectoryDialog.tsx) (shares `ConvertDialog.css`) | Directory-level preview dialog: skip-processed checkbox only (no profile picker — the job uses the default layout preset and current global settings); posts `POST /api/jobs/preview-directory`. |
| [`frontend/src/components/TagDirectoryDialog.tsx`](../frontend/src/components/TagDirectoryDialog.tsx) (shares `ConvertDialog.css`) | Directory-level tag dialog: skip-processed checkbox only (provider/vocabulary come from Settings, not per-job); posts `POST /api/jobs/tag-directory`. |
| [`frontend/src/components/BackendStatusPanel.tsx`](../frontend/src/components/BackendStatusPanel.tsx) | Displays `/api/health` and `/api/app/info` (app version, DB schema version, ffmpeg availability); shown in `AppLayout`'s empty state while no source is connected. |
| [`frontend/src/context/PreviewVisibilityContext.tsx`](../frontend/src/context/PreviewVisibilityContext.tsx) | `previewsVisible` boolean + toggle, session-only (not persisted); gates real thumbnail rendering (vs. folder/film icon fallback) in `LibraryView.tsx`, purely client-side. |
| [`frontend/src/context/InterfaceSettingsContext.tsx`](../frontend/src/context/InterfaceSettingsContext.tsx) | Owns `theme` (`strict`/`playful`/`casino` — `casino` added per user request as a third, deliberately loud preset alongside the existing two, not a replacement — applied as `document.documentElement.dataset.theme`) and `language` together, both backed by `GET/PUT /api/interface-settings`: a `localStorage`/browser-locale guess is applied synchronously on first render (avoids a flash of the wrong theme/language), then reconciled against the backend-persisted values once that request resolves. `setTheme()`/`setLanguage()` update local state, `i18next`, `localStorage`, and the backend in one call each — instant, no reload. Wraps the whole app in `App.tsx`, outermost provider. |
| [`frontend/src/context/SourceContext.tsx`](../frontend/src/context/SourceContext.tsx) | Fetches and holds the active source (`GET /api/source`); exposes `refresh()` and `setSource()` (used right after connecting/replacing a source) to the whole app. |
| [`frontend/src/context/JobsContext.tsx`](../frontend/src/context/JobsContext.tsx) | Polls `GET /api/jobs` every 1.5s for the whole app (shared by the top bar and `JobsModal` so they don't poll independently); while a job is queued/running, also polls that job's `GET /api/jobs/{id}/items` on the same interval for the activity indicator's "current item" tooltip. Exposes `jobs`, `activeJob`, `activeJobItems`, `refresh()`. |
| [`frontend/src/context/ConversionProfilesContext.tsx`](../frontend/src/context/ConversionProfilesContext.tsx) | Fetches and holds `GET /api/conversion-profiles` for the whole app; exposes `profiles`, `loading`, `refresh()`, and `ensureDefaultProfile(name)` (user request — returns the existing default/first profile, or `POST`s a starter one — `h265`/`mp4`/1024px/CRF 28/drop-audio — and marks it `is_default` if the list is empty, re-fetching fresh from the API rather than trusting the context's possibly-still-loading `profiles` state). Shared by the Settings profile-management section, both directory convert dialogs, and `FileConvertModal.tsx`/`FileTuneModal.tsx` so they stay in sync after create/edit/delete/promote. |
| [`frontend/src/context/TagsContext.tsx`](../frontend/src/context/TagsContext.tsx) | Fetches and holds `GET /api/tags` (the full vocabulary) for the whole app; exposes `tags`, `loading`, `refresh()`. Shared by `TaggingSettingsSection` (vocabulary editor) and `LibraryView`'s search box (client-side prefix filtering for autocomplete) so both stay in sync after add/rename/deactivate/delete. |
| [`frontend/src/hooks/useBackendStatus.ts`](../frontend/src/hooks/useBackendStatus.ts) | Fetches health and app-info endpoints; exposes a loading/error/ready state. |
| [`frontend/src/utils/format.ts`](../frontend/src/utils/format.ts) | `formatSize(bytes)` — shared byte-count formatter used by `LibraryCards.tsx` and `FileInfoPanel.tsx`. `formatBitrate(bitsPerSecond)`/`formatDuration(seconds)` (user request) — Kbps/Mbps and `h:mm:ss`/`m:ss` formatters for `FileInfoPanel.tsx`'s media-info grid. Unit-tested in `format.test.ts` (vitest). |
| [`frontend/src/utils/layoutGeometry.ts`](../frontend/src/utils/layoutGeometry.ts) | Pure grid-geometry helpers for the preview layout editor (no DOM/network), extracted from `PreviewSettingsSection.tsx`: `cellsOf()`/`buildOccupancy()`/`fillAll()`/`computeTilesLocal()` — the client-side mirror of the backend's canonical `compute_layout_tiles()`, used for instant editor feedback and preset-gallery thumbnails (the backend endpoint stays the source of truth for validation). Unit-tested in `layoutGeometry.test.ts` (vitest). |
| [`frontend/src/i18n/index.ts`](../frontend/src/i18n/index.ts) | i18next setup; detects initial language from a persisted preference or the browser locale (falls back to English) for the first paint, reconciled against the backend by `InterfaceSettingsContext.tsx` ([Stage 9](./spec/roadmap.md#stage-9--polish-and-optional-features)) once that loads. Language is selected directly (not cycled) from `SettingsModal`. |
| [`frontend/src/i18n/locales/en.json`](../frontend/src/i18n/locales/en.json), [`ru.json`](../frontend/src/i18n/locales/ru.json) | UI string resources; kept in parity (see [Roadmap Cross-Stage Rules](./spec/roadmap.md#cross-stage-rules)). |
| [`frontend/src/types/api.ts`](../frontend/src/types/api.ts) | TypeScript types for health/app-info, source config (`SourceConfig.host`/`.port` for `smb`, [Stage 7](./spec/roadmap.md#stage-7--smb-source-and-playback)), directory tree, directory-children (`DirectoryEntry.has_folder_preview`), file-entry, job/job-item/log-event, conversion-profile, variant-override, preview (`PreviewLayoutPreset`, `PreviewSettings`, `EnlargedTile`, `LayoutTile`, `TimelineFlow`, `AspectRatioMode`, `LayoutPreviewResponse`), tagging (`Tag`, `FileTagAssignment`, `TaggingSettings`, `ProviderName`, `ProviderConfig`), playback (`PlaybackMode`, `PlaybackSettings`, `PlaybackInfo`), backup (`BackupSummary`, `BackupSettings`, [Stage 8](./spec/roadmap.md#stage-8--backup-restore-maintenance-source-switching)), and `ThemePreset`/`InterfaceSettings`, `SimilarFile` response/request shapes. Also exports the runtime `THEME_PRESETS` tuple (`['strict', 'playful', 'casino']`, `ThemePreset` is derived from it) — the single source of truth iterated by both `TopBar.tsx`'s cycle toggle and `InterfaceSection.tsx`'s picker, so a new preset added there (plus icon entries and `theme.*` locale keys) surfaces everywhere at once. |
