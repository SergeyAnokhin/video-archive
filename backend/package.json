{
  "name": "video-archive-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "python -m app.main",
    "test": "python -m unittest discover -s tests -p \"test_*.py\""
  }
}
